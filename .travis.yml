os:
  - linux
#  - osx
dist: trusty
sudo: required
language: python
cache: pip
python:
  - "2.7"
  - "3.6"

install:
  - ./.travis/install.sh

script:
  - ./.travis/build.sh

cache:
  - directories:
    - $HOME/.build_cache
    - $HOME/.ccache

env:
  - ONNX_BRANCH=master
  - ONNX_BRANCH=rel-1.1.2
